from distutils.core import setup, find_packages

setup(name='H Pip',
      version='1.0',
      description='A pip package for adding quick code into repositories when debugging, logging, etc.',
      author='',
      author_email='',
      url='',
      packages=find_packages(),
     )